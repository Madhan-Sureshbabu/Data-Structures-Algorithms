#include<bits/stdc++.h>
using namespace std;

int main()
{
	// string s1 = "+";
	// if (s1=="+")
	// 	cout<<"TTT"<<endl;
	// vector<string> v = {"9","10","2"};
	// sort(v.begin(),v.end());
	// for (auto s : v)
	// 	cout<<s;
	int n = 10;
	vector<int> adj[n];
	for (int i=0;i<n;i++)
	{
		adj[i].push_back(i);
	}

	// cout<<stoi(s1)<<endl;
}